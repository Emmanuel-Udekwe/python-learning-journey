# covert a float input to int and show the difference
num1 = float(input ("enter a decimal number:"))
converted = int (num1)
print ("the original float is " + str (num1))
print ("the converted integer is " + str(converted))
